<p id="notice"><%= notice %></p>

<h1>Listing Adverts</h1>
<button class="standard-button"> <%= link_to 'Home', root_path  %></button>

    <% @adverts.each do |advert| %>
    <dialog id="myDialog">This is a dialog window</dialog>
        <p><%= advert.name %></p>
        <p><%= image_tag advert.avatar %></p>
        <p><%= advert.availability %></p>
        <!-- button goes here -->
        <!-- wrap this in modal -->
           <!--  <p><%= advert.name %></p>
            <p><%= advert.sector %></p>
            <p><%= advert.address %></p>
            <p><%= image_tag advert.avatar %></p>
            <p><%= advert.size %></p>
            <p><%= advert.facilities %></p>
            <p><%= advert.seller_id %></p>
            <p><%= advert.date_time %></p>
            <p><%= advert.availability %></p> -->
        <!-- wrap this in modal -->

     <%= link_to 'More Info', advert, class: "btn btn-default"%>
      
      <p><%= link_to 'Edit', edit_advert_path(advert) %></p>
        
      <p><%= link_to 'Destroy', advert, method: :delete, data: 
      { confirm: 'Are you sure?' } %></p>
  
    <% end %>


    <!-- Button trigger modal -->
    <button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#myModal">
      More Info
    </button>

    <!-- Adveert show page modal -->
    <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <h4 class="modal-title" id="myModalLabel"> Individual Listing Title</h4>
          </div>
          <div class="modal-body">
            <p> Advert listing info here </p>
            <% @adverts.each do |advert| %>
            <p><%= advert.name %></p>
             <p><%= advert.sector %></p>
             <p><%= advert.address %></p>
             <p><%= image_tag advert.avatar %></p>
             <p><%= advert.size %></p>
             <p><%= advert.facilities %></p>
             <p><%= advert.seller_id %></p>
             <p><%= advert.date_time %></p>
             <p><%= advert.availability %></p>
            <% end %> 
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Back</button>
            <button type="button" class="btn btn-primary">Make a bid (new)</button>
          </div>
        </div>
      </div>
    </div>



<% link_to 'New Advert', new_advert_path %>
